/**
 * audio.js — AudioManager
 *
 * מנהל קריינות לכל מסך תוכן.
 * Graceful fallback: אם קובץ חסר — שתיקה, ללא שגיאה.
 * Autoplay: ממתין ל-user gesture ראשון.
 *
 * PLACEHOLDER — לא מיושם עדיין
 */

class AudioManager {
  // TODO
}
